{
  "name": "promptahead",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "vite build --config extension/vite.config.ts",
    "typecheck": "tsc -p tsconfig.spikes.json && tsc -p tsconfig.extension.json",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npm run build && playwright test",
    "test:e2e:headed": "npm run build && HEADED=1 playwright test",
    "test:ci": "npm run typecheck && npm run lint && npm run test && npm run build && playwright test",
    "spikes:build": "vite build --config spikes/vite.config.ts",
    "spikes:dev": "vite build --config spikes/vite.config.ts --watch"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@crxjs/vite-plugin": "^2.0.0-beta.31",
    "@eslint/js": "^9.19.0",
    "@playwright/test": "^1.62.1",
    "@types/chrome": "^0.0.304",
    "@types/dom-chromium-ai": "^0.0.9",
    "@types/jsdom": "^28.0.3",
    "@types/node": "^22.13.1",
    "eslint": "^9.19.0",
    "eslint-config-prettier": "^10.0.1",
    "jsdom": "^29.1.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.22.0",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  }
}
